const numbrBtns = document.querySelectorAll('[data-number');
const oprtrBtns = document.querySelectorAll('[data-operator]');
const decimalBtn = document.getElementById('decimalBtn');
const equalBtn = document.getElementById('equalBtn');
const clearBtn = document.getElementById('clear');
const deleteBtn = document.getElementById('delete');

const currentScrn = document.querySelector('.current-input');
const lastScrn = document.querySelector('.last-input');